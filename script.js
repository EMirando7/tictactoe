const tictactoe = {
    initalize: (function () {
        'use strict';
        let gameBoard = [];

    }),
    //
    players: function (player) {
        //return what what is to be used in the newly created object
        return {player}
    }
}